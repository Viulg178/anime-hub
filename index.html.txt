<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HiAnime Private Stream</title>
    <style>
        /* Dark Anime-Themed Styles */
        body {
            background-color: #0b0b0d;
            color: #ffffff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 20px;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 2px solid #ff4500;
            padding-bottom: 15px;
            margin-bottom: 30px;
        }

        h1 {
            color: #ff4500;
            margin: 0;
            font-size: 28px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .container {
            max-width: 1100px;
            margin: 0 auto;
        }

        /* Video Player Area */
        .player-container {
            background-color: #141417;
            border-radius: 8px;
            padding: 10px;
            margin-bottom: 40px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.5);
            display: none; /* Hidden until a video card is clicked */
        }

        .player-title {
            font-size: 20px;
            color: #ff4500;
            margin: 10px 0;
            font-weight: 600;
        }

        video {
            width: 100%;
            border-radius: 4px;
            background: #000;
        }

        /* Anime Catalog Grid */
        h2 {
            font-size: 22px;
            margin-bottom: 20px;
            border-left: 4px solid #ff4500;
            padding-left: 10px;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 20px;
        }

        .anime-card {
            background-color: #141417;
            border-radius: 6px;
            overflow: hidden;
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s;
            border: 1px solid #232329;
        }

        .anime-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(255, 69, 0, 0.3);
            border-color: #ff4500;
        }

        .thumbnail {
            width: 100%;
            height: 280px;
            background-color: #232329;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #666;
            font-size: 14px;
            position: relative;
        }

        /* Custom play triangle icon on thumbnail */
        .thumbnail::after {
            content: '▶';
            font-size: 40px;
            color: rgba(255, 255, 255, 0.7);
            position: absolute;
            transition: color 0.2s;
        }

        .anime-card:hover .thumbnail::after {
            color: #ff4500;
        }

        .info {
            padding: 12px;
        }

        .info h3 {
            margin: 0 0 5px 0;
            font-size: 16px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .info p {
            margin: 0;
            font-size: 13px;
            color: #888;
        }
    </style>
</head>
<body>

    <div class="container">
        <header>
            <h1>HiAnime <span style="color:#fff;">Private</span></h1>
            <span style="color: #888; font-size: 14px;">Streaming Base v1.0</span>
        </header>

        <div class="player-container" id="videoPlayerBox">
            <video id="mainVideoPlayer" controls autoplay></video>
            <div class="player-title" id="currentPlayerTitle">Now Playing</div>
        </div>

        <h2>My Uploaded Anime</h2>
        <div class="grid">

            <div class="anime-card" onclick="playVideo('Apothecary Diaries Season 1 Episode 1', 'https://telegram-file-link-bot-production-3194.up